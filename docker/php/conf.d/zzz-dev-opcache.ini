; Dev-only overrides mounted via compose.yaml (loads after docker-php-custom-php.ini).
; Without this, opcache.validate_timestamps=0 (see php.ini) freezes PHP bytecode:
; edits to src/ are ignored until the FrankenPHP container restarts.
opcache.validate_timestamps=1
opcache.revalidate_freq=0
